import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import DateRangeVariants from '.';

describe('<DateRangeVariants />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <DateRangeVariants />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<DateRangeVariants className="test-class-name" />);
    expect(wrapper).to.have.className('date-range-variants');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
